# 독서 기록 · 통계 · 계획 로컬 웹앱

작성일: 2026-07-30

## 배경

독서를 기록하고, 얼마나 읽었는지 통계로 확인하고, 독서 계획(목표)을 세워 진척도를 추적하는 프로그램. 데이터는 전부 로컬에 보관한다(클라우드 서비스 없음).

`C:\Users\user\Desktop\test`는 현재 빈 디렉터리이고, git 저장소가 아니다. 기존 코드가 없으므로 처음부터 만든다. 환경: Python 3.12.5, Node 24 사용 가능(Node는 쓰지 않음), git 미설치.

대화로 확정한 요구사항:

| 항목 | 결정 |
|---|---|
| 실행 형태 | 로컬 웹 서버 + 브라우저 |
| 접속 범위 | 같은 와이파이의 폰/태블릿에서도 접속 |
| 기록 단위 | 완독 기록 + 일별 세션 기록(진도 갱신 시 자동 생성) |
| 목표 | ① 연간 권수 ② 일일 페이지/시간 ③ 책별 마감일 |
| 책 정보 입력 | 알라딘 OpenAPI 자동조회 + 수동 보완 |
| 추가 기능 | 읽고싶은 책(위시리스트), 읽는 중 간단 메모, 완독 책 별점 |

핵심 설계 판단 — **"세션 기록"이라는 별도 입력 화면을 만들지 않는다.** 책 카드에서 현재 페이지를 `412 → 455`로 갱신하면 서버가 `2026-07-29 / 사피엔스 / +43p` 세션 행을 자동으로 쌓는다. 입력 부담 없이 일일 통계·연속 독서일(streak)이 확보된다. 과거에 이미 다 읽은 책은 진도 없이 완독 등록만 해도 되며, 그 책은 일일 통계에 잡히지 않는다.

## 기술 스택

- **Python 3.12 + FastAPI + uvicorn** — 로컬 서버
- **Jinja2 서버 렌더링** + 바닐라 JS(진도 갱신, 알라딘 검색 모달만 fetch) — 빌드 스텝 없음
- **SQLite**(stdlib `sqlite3`) — 파일 1개, 백업은 복사 한 번
- **차트는 직접 만든 인라인 SVG** — CDN/외부 라이브러리 0개. 인터넷이 끊겨도, 5년 뒤에도 그대로 뜬다
- **httpx** — 알라딘 API 호출

의존성: `fastapi`, `uvicorn[standard]`, `jinja2`, `httpx`, `python-multipart`. venv에 설치.

> 차트 코드를 처음 작성하기 전에 `dataviz` 스킬을 먼저 읽는다(색상 팔레트·축·툴팁 규칙).

## 파일 구조

```
C:\Users\user\Desktop\test\
  run.bat                  # venv 활성화 → uvicorn 실행 → 브라우저 열기
  requirements.txt
  README.md                # 알라딘 TTB키 발급법, 방화벽 예외, 백업 방법
  app/
    main.py                # FastAPI 앱, 라우터 등록, static/templates 마운트
    db.py                  # 연결, 스키마 생성, 스키마 버전 마이그레이션
    schemas.py             # pydantic 모델
    aladin.py              # 알라딘 클라이언트 (검색 + 상세)
    services/
      progress.py          # 진도 갱신 → 세션 생성/수정 로직
      stats.py             # 통계 집계 쿼리
      goals.py             # 목표 진척·페이스·예상 완독일 계산
    routers/
      pages.py             # HTML 페이지 5개
      books.py  sessions.py  goals.py  stats.py  settings.py   # JSON API
    templates/
      base.html  dashboard.html  library.html  book.html
      stats.html  settings.html
      _partials/  (book_card.html, rating.html, progress_bar.html)
    static/
      app.css  app.js  charts.js
  data/
    reading.db             # 자동 생성
    config.json            # TTB키 등 (README에 "공유 금지" 명시)
```

## DB 스키마

```sql
CREATE TABLE books (
  id            INTEGER PRIMARY KEY,
  title         TEXT NOT NULL,
  author        TEXT,
  publisher     TEXT,
  isbn13        TEXT UNIQUE,
  cover_url     TEXT,
  category      TEXT,              -- 소설/경제/기술...
  total_pages   INTEGER,           -- 알라딘 미제공 시 NULL → 수동 입력 유도
  current_page  INTEGER NOT NULL DEFAULT 0,
  status        TEXT NOT NULL      -- 'wishlist'|'reading'|'done'|'paused'
                CHECK (status IN ('wishlist','reading','done','paused')),
  rating        INTEGER CHECK (rating BETWEEN 1 AND 5),   -- 완독 시
  memo          TEXT,              -- 읽는 중 간단 메모
  started_on    TEXT,              -- 'YYYY-MM-DD'
  finished_on   TEXT,
  due_date      TEXT,              -- 책별 마감일 목표
  created_at    TEXT NOT NULL,
  updated_at    TEXT NOT NULL
);

CREATE TABLE sessions (
  id          INTEGER PRIMARY KEY,
  book_id     INTEGER NOT NULL REFERENCES books(id) ON DELETE CASCADE,
  log_date    TEXT NOT NULL,       -- 'YYYY-MM-DD'
  start_page  INTEGER NOT NULL,
  end_page    INTEGER NOT NULL,
  pages       INTEGER NOT NULL,    -- end - start (되돌리기 시 음수 가능)
  minutes     INTEGER,             -- 선택 입력
  note        TEXT
);
CREATE INDEX idx_sessions_date ON sessions(log_date);
CREATE INDEX idx_sessions_book ON sessions(book_id);

CREATE TABLE goals (
  id       INTEGER PRIMARY KEY,
  kind     TEXT NOT NULL          -- 'yearly_books'|'daily_pages'|'daily_minutes'
           CHECK (kind IN ('yearly_books','daily_pages','daily_minutes')),
  period   TEXT,                  -- yearly: '2026' / daily: NULL
  target   INTEGER NOT NULL,
  active   INTEGER NOT NULL DEFAULT 1,
  UNIQUE(kind, period)
);

CREATE TABLE settings (key TEXT PRIMARY KEY, value TEXT);  -- schema_version, ttb_key 등
```

책별 마감일은 `books.due_date` 하나로 처리한다(별도 테이블 불필요). 위시리스트도 `status='wishlist'`로 처리해 테이블을 늘리지 않는다.

## 화면

**① 대시보드 `/`**
- 연간 목표 링: `14 / 24권 (58%)` + "이 페이스면 연말 21권 예상"
- 오늘 일일 목표: `23 / 30p` 바 + 연속 12일 🔥
- 읽는 중인 책 카드 — 표지·진도바·**인라인 페이지 입력창**(여기서 바로 진도 갱신) + 마감일 있으면 `남은 34일 / 하루 21p 필요`
- 최근 7일 미니 막대, 최근 세션 목록

**② 서재 `/library`** — 탭(읽는 중 / 완독 / 읽고싶은 / 중단), 검색, 정렬, `+ 책 추가` → 알라딘 검색 모달

**③ 책 상세 `/books/{id}`** — 메타 정보, 진도 누적 선그래프, 세션 목록(수정·삭제), 메모, 별점, 마감일 설정, 상태 전환 버튼

**④ 통계 `/stats`** — 월별 권수/페이지 막대, 카테고리 파이, 연간 잔디 히트맵, 저자·출판사 순위, 별점 분포, 평균 완독 소요일

**⑤ 설정 `/settings`** — 알라딘 TTB키, 목표 3종 설정, CSV 내보내기, DB 백업 버튼

## 핵심 로직

**진도 갱신** (`services/progress.py`)
```
PATCH /api/books/{id}/progress  { current_page, minutes?, note? }
  1. old = books.current_page
  2. 같은 책 · 같은 날짜 세션이 있으면 end_page/pages를 갱신(하루에 여러 번 눌러도 행 1개)
     없으면 새 세션 INSERT
  3. books.current_page 갱신
  4. status='wishlist' 였고 page>0 → 'reading' + started_on=today 자동 전환
  5. current_page >= total_pages → 완독 확인 모달 → 'done' + finished_on + 별점 입력
```

**목표 계산** (`services/goals.py`)
- 연간 진척: `count(finished_on LIKE '2026-%')` / target, 페이스 예상 = 경과일 대비 선형 외삽
- 일일 달성: 오늘 `sum(pages)` / `sum(minutes)` vs target
- streak: `log_date DISTINCT` 역순 순회 (일일 목표 설정 시 "목표 달성한 날"만 카운트, 미설정 시 "읽은 날")
- 책별 마감: `(total_pages - current_page) / max(1, due_date - today)` = 하루 필요 페이지. 최근 7일 실제 속도와 비교해 `예상 완독일` 표시, 마감 초과 시 빨간 경고

**알라딘 연동** (`aladin.py`)
- 검색: `ItemSearch.aspx` (Query, SearchTarget=Book, output=js, Version=20131101)
- 상세: `ItemLookUp.aspx` (itemIdType=ISBN13, **OptResult=packing** → `subInfo.itemPage`에 총 페이지)
- **페이지 수가 없는 책이 있다.** 조회 실패·페이지 누락·TTB키 미설정 모두 조용히 수동 입력 폼으로 폴백하고, 앱은 정상 동작해야 한다. 이 폴백 경로를 반드시 테스트한다.
- TTB키는 `data/config.json`에 저장, 코드에 하드코딩 금지

**LAN 접속** — `uvicorn --host 0.0.0.0 --port 8765`. 인증은 없으므로 README에 "신뢰하는 홈 네트워크에서만 사용" 명시. 모바일 레이아웃은 CSS 미디어쿼리로 1컬럼 대응(진도 입력창은 손가락으로 누를 수 있는 크기로).

## 작업 순서

1. **스캐폴드 + DB + 책 CRUD** — `run.bat`으로 서버 뜨고, 수동 입력으로 책 등록/서재 목록/상태 전환(위시리스트 포함)까지
2. **진도 → 세션 + 책 상세** — 인라인 진도 갱신, 세션 자동 생성/수정/삭제, 완독 전환 + 별점 + 메모
3. **목표 3종 + 대시보드** — 설정 화면, 진척·페이스·streak·마감일 계산, 대시보드 조립
4. **통계 + SVG 차트** — `dataviz` 스킬 먼저 읽고 `charts.js` 작성 (막대·파이·선·히트맵)
5. **알라딘 연동** — 검색 모달, 자동 채우기, 폴백 경로
6. **마무리** — 모바일 CSS, CSV 내보내기, DB 백업, README(TTB키 발급·방화벽 예외·백업)

## 검증

각 단계마다 실제로 서버를 띄워 확인한다.

- **샘플 데이터**: `scripts/seed.py`로 책 15권 + 90일치 세션을 생성해 통계·차트·streak가 빈 화면이 아닌 상태로 검증
- **경계 케이스 직접 확인**:
  - `total_pages`가 NULL인 책 → 진도바·마감일 계산이 깨지지 않는지
  - 진도를 되돌렸을 때(455 → 420) 음수 세션 처리
  - 하루에 같은 책 진도를 3번 갱신 → 세션 행이 1개로 합쳐지는지
  - 세션이 하나도 없는 날 → streak 끊김 표시
  - TTB키 미설정 상태에서 `+ 책 추가` → 수동 폼으로 폴백
  - 연도 경계(1/1) 연간 목표 롤오버
- **API 확인**: `Invoke-RestMethod http://127.0.0.1:8765/api/stats/summary` 로 집계값이 화면 표시와 일치하는지 대조
- **모바일**: 폰 브라우저로 `http://<PC-IP>:8765` 접속 → 대시보드에서 진도 갱신이 되는지
- **데이터 보존**: 서버 재시작 후에도 기록 유지, `data/reading.db` 복사본으로 복원 가능한지
